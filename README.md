# Домашнее задание по Zabbix 2

В рамках работы был развернут Zabbix Server с использованием PostgreSQL, настроены Zabbix Agents, созданы кастомные шаблоны мониторинга и дашборд.

---

## Задание 1. Создание собственного шаблона

### Цель
Создать собственный шаблон Zabbix, который собирает данные о загрузке CPU и RAM хоста в процентах.

### Выполнение
1. В веб-интерфейсе Zabbix перешёл в раздел **Data collection → Templates**
2. Создал новый шаблон с названием **Custom CPU RAM**
3. В шаблоне создал элементы данных:
   - **CPU utilization (%)**
     - Тип: `Calculated`
     - Формула: `100 - system.cpu.util[idle]`
   - **RAM utilization (%)**
     - Тип: `Calculated`
     - Формула: `100 - vm.memory.size[pavailable]`
4. Убедился, что элементы данных успешно создаются и не имеют статуса *Not supported*

### Результат
Создан кастомный шаблон для мониторинга CPU и RAM в процентах.

📸 **Скриншот:**
- `task1_template.png` — страница шаблона «Задание 1» с элементами данных

---

## Задание 2. Добавление хостов

### Цель
Добавить в Zabbix два хоста и назначить им имена по шаблону `<фамилия и инициалы>-1` и `<фамилия и инициалы>-2`.

### Выполнение
1. Установлен Zabbix Agent на две виртуальные машины:
   - Zabbix Server
   - Отдельный Linux-хост
2. В конфигурации агентов указан Zabbix Server в параметре `Server`
3. В веб-интерфейсе Zabbix:
   - Перешёл в **Data collection → Hosts**
   - Добавил два хоста:
     - `SolonkoDK1`
     - `SolonkoDK2`
4. Для каждого хоста:
   - Указан IP-адрес
   - Назначена группа **Linux servers**
   - Назначен шаблон **Linux by Zabbix Agent**

### Результат
Оба хоста успешно добавлены и отображаются в списке хостов.

📸 **Скриншот:**
- `task2_hosts.png` — список хостов в разделе *Configuration → Hosts*

---

## Задание 3. Привязка шаблонов

### Цель
Привязать созданный шаблон и стандартный шаблон Linux ко всем хостам.

### Выполнение
1. Для каждого хоста в разделе **Templates**:
   - Привязан шаблон **Linux by Zabbix Agent**
   - Привязан кастомный шаблон **Custom CPU RAM**
2. Проверено, что:
   - Статус хостов — **Enabled**
   - Availability — зелёный индикатор **ZBX**
3. В разделе **Monitoring → Latest data** появились данные CPU и RAM

### Результат
Все шаблоны успешно привязаны, данные поступают корректно.

📸 **Скриншоты:**
- `task3_templates.png` — привязка шаблонов к хостам
- `task3_latest_data.png` — данные в разделе *Latest data*

---

## Задание 4. Создание дашборда

### Цель
Создать кастомный дашборд с графиками мониторинга.

### Выполнение
1. В разделе **Dashboards** создан новый дашборд с названием **DZ4**
2. На дашборд добавлены графики:
   - CPU utilization — `SolonkoDK1`
   - CPU utilization — `SolonkoDK2`
   - RAM utilization — `SolonkoDK1`
   - RAM utilization — `SolonkoDK2`
3. Проверено, что графики отображают актуальные данные

### Результат
Создан кастомный дашборд с мониторингом CPU и RAM.

📸 **Скриншот:**
- `task4_dashboard.png` — дашборд «Задание 4»

---

## Чек-лист перед сдачей

- [ ] Все хосты имеют статус **Enabled**
- [ ] Availability у хостов — зелёный **ZBX**
- [ ] В *Latest data* отображаются значения CPU и RAM
- [ ] Графики на дашборде строятся
- [ ] Все скриншоты добавлены в репозиторий
- [ ] README.md оформлен в формате Markdown

---

## Используемые технологии

- Zabbix Server 7.x
- Zabbix Agent
- PostgreSQL
- Debian Linux
- Oracle VirtualBox
